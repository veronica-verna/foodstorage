#### function for calculating regressions for groups ####

group_reg <- function(group) {
  
  len <- length(group)
  
  ## if group is bigger than 12, result will be a data frame ###
  
  if (len > 12) {
    
  }
  
}